spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: pgadmin
    password: pgpwd
# NOTE: Below aren't required
#    platform: postgres
#    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    generate-ddl: true
# NOTE: Below aren't required
#    database-platform: org.hibernate.dialect.PostgreSQLDialect
#    hibernate:
#      ddl-auto: create
